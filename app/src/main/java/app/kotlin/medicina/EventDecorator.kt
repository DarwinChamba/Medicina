package app.kotlin.medicina

import android.graphics.Color
/*
import android.text.style.ForegroundColorSpan
import com.prolificinteractive.materialcalendarview.CalendarDay
import com.prolificinteractive.materialcalendarview.DayViewDecorator
import com.prolificinteractive.materialcalendarview.DayViewFacade

class EventDecorator(private val dates: Collection<CalendarDay>, private val color: Int) : DayViewDecorator {

    override fun shouldDecorate(day: CalendarDay): Boolean {
        return dates.contains(day) // Verifica si la fecha está en la lista
    }

    override fun decorate(view: DayViewFacade) {
        view.addSpan(ForegroundColorSpan(color)) // Cambia el color del texto
    }
}
*/